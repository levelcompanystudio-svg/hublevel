{
  "name": "hublevel-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "HubLevel backend - base para integracoes Meta Ads e Google Ads (Bloco 1: estrutura, sem chamadas externas reais).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "check": "tsc --noEmit",
    "lint": "oxlint src"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "oxlint": "^1.71.0",
    "tsx": "^4.19.2",
    "typescript": "^6.0.2"
  }
}
