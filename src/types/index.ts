/**
 * HubLevel - Shared TypeScript types.
 *
 * This barrel file will re-export all shared types as the project grows.
 * For now it's a placeholder to establish the types/ directory convention.
 */

export {};
